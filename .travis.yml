sudo: true

language: go

services:
  - docker

before_script:
  - docker pull dannypsnl/elz-test-env

script:
  - docker run --rm dannypsnl/elz-test-env /bin/bash -c "cd /go/src/github.com/elz-lang/elz; go test -v ./..."
