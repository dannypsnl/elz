sudo: true

language: rust

rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

#after_success:
 #- bash <(curl -s https://codecov.io/bash)

